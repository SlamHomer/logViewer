import * as React from "react";

/**
 * TODO: comment me
 */
const LogText = (props) => {
    return (
        <div>
            {props.lastBCAndFormattedText.formattedText}
        </div>
    );
};

export default LogText;